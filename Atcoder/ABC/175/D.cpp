#include <bits/stdc++.h>
using namespace std ;
using V_2 = vector<vector<int>> ;
using ll = long long ;

int n , k ;

int main(){
  cin >> n >> k ;
  int p[n] , c[n] ;
  for(int i = 0 ; i < n ; i ++) cin >> p[i] ;
  for(int i = 0 ; i < n ; i ++) cin >> c[i] ; 
  V_2 dp(k+1,vector<int>(n+1,0))
}